name: 执行lv_work脚本3

on:
  schedule:
    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 * * * *'

jobs:
  work:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
        
    - name: 安装依赖包
      run: pip3 install requests
        
    - name: 执行脚本
      run: python3 lv_work.py 
